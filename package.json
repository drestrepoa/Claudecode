{
  "name": "inbox-agent-workshop",
  "private": true,
  "type": "module",
  "description": "Build-your-own-agent training demo for lawyers: a 500-email inbox and the agent that organises it",
  "scripts": {
    "build": "python3 data/import_xlsx.py && python3 build.py",
    "dev": "netlify dev"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0"
  }
}
